# Layout plano de node_modules (como npm). Maximiza compatibilidad con el
# CLI de Amplify (ampx) y AWS CDK, que asumen dependencias hoisteadas.
node-linker=hoisted
# Permite ejecutar scripts de build de dependencias nativas (esbuild, etc.).
enable-pre-post-scripts=true
